# Intentionally pinned to vulnerable versions so Trivy has something to find.
# DO NOT copy into a real project.
flask==1.0        # multiple CVEs in older Flask/Werkzeug
pyyaml==5.3.1     # CVE-2020-14343 (CRITICAL) - arbitrary code execution
requests==2.19.0  # CVE-2018-18074 (HIGH)     - Authorization header leak
urllib3==1.24.1   # CVE-2019-11324, CVE-2019-11236 (HIGH)
jinja2==2.10      # CVE-2019-10906 (HIGH)     - sandbox escape
